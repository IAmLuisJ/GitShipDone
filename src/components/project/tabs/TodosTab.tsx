import { ProjectTabPlaceholder } from "./ProjectTabPlaceholder";

export function TodosTab() {
  return (
    <ProjectTabPlaceholder
      title="Todos"
      description="Project tasks, urgency, and completion controls will appear here."
      testId="todos-tab"
    />
  );
}
