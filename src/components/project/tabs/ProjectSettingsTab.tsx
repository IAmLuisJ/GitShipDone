import { ProjectTabPlaceholder } from "./ProjectTabPlaceholder";

export function ProjectSettingsTab() {
  return (
    <ProjectTabPlaceholder
      title="Settings"
      description="Project settings, sharing, integrations, and danger zone controls will appear here."
      testId="settings-tab"
    />
  );
}
